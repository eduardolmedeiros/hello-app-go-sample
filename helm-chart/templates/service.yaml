apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-app-go-sample.fullname" . }}
  labels:
    {{- include "hello-app-go-sample.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello-app-go-sample.selectorLabels" . | nindent 4 }}
